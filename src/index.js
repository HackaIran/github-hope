/**
 * Represents main hope function
 * @param {string} repoURL url which will be checked
 * @returns {Promise} a promise including test results
 */
export default function hope (repoURL) {

    return new Promise ((resolve, reject) => {

    })
    
}